<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Disabled -->
    <item android:state_enabled="false">
        <shape>
            <solid android:color="@color/vuzix_dark_red" />
            <corners android:radius="5dp" />
        </shape>
    </item>

    <!-- Pressed -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="@color/vuzix_medium_red" />
            <corners android:radius="5dp" />
        </shape>
    </item>

    <!-- Default color -->
    <item>
        <shape>
            <solid android:color="@color/vuzix_red" />
            <corners android:radius="5dp" />
        </shape>
    </item>


</selector>